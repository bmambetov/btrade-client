<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- TODO Remove the following "exclude" elements to make them a part of the auto backup -->
    <!-- Exclude the shared preferences file that contains the GCM registrationId -->
    <exclude
        domain="sharedpref"
        path="GroceryLoginPrefs2.xml" />
    <exclude
        domain="sharedpref"
        path="GroceryLoginPrefs.xml" />
    <exclude
        domain="sharedpref"
        path="lan.xml" />
    <exclude
        domain="sharedpref"
        path="GoGrocer.xml" />
</full-backup-content>
